{
  "name": "kevinselwyn.com",
  "version": "1.0.0",
  "description": "Personal website that builds/deploys to kevinselwyn.github.io",
  "main": "index.js",
  "repository": "https://github.com/kevinselwyn/kevinselwyn.com",
  "author": "Kevin Selwyn <kevinselwyn@gmail.com>",
  "license": "GPL-3.0",
  "private": false,
  "scripts": {
    "dev": "hugo serve --buildDrafts --port 1234",
    "serve": "cd public && python -m SimpleHTTPServer 1234",
    "build": "hugo",
    "package": "yarn run package:html && yarn run package:xml",
    "package:html": "find public -name '*.html' -exec html-minifier --collapse-whitespace --minify-css inline {} --output {} \\;",
    "package:xml": "find public -name '*.xml' -exec html-minifier --collapse-whitespace --minify-css inline {} --output {} \\;",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "html-minifier": "^3.5.21"
  }
}
